kind: BuildConfig
apiVersion: build.openshift.io/v1
metadata:
  name: greeting-console
spec:
  nodeSelector: {}
  strategy:
    type: Docker
  source:
    type: Git
    git:
      uri: 'git@github.com:larryklean/do400-greeting-console.git'
      ref: main
  output:
    to:
      kind: DockerImage
      name: quay.io/repository/oolaleyee/greeting-console
    pushSecret:
      name: quay-credentials
